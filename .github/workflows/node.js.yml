# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Node.js CI

on:
  push:
    branches: ["main"]
jobs:
  build: 
    runs-on: [noon-iraq]

    strategy:
      matrix:
        node-version: [20.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          # cache: "npm"

      - run: NODE_OPTIONS=--max_old_space_size=4096 npm i --force

      - run: npm run build --if-present
      # - name: Pm2 Start
        # run: pm2 restart dashboard-noon-iraq
      # - run: pm2 describe dashboard-noon-iraq
      # - run: pm2 logs dashboard-noon-iraq
      - run: PORT=3000 pm2 start npm  --name "dashboard-noon-iraq-app" -- start
